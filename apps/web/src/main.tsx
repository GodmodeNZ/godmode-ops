import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import './styles.css';
type InventoryRow={skuId:string;sku?:{code:string;name:string;family:{name:string;category:string}};locationId:string;onHand:number;reserved:number;available:number};
type Build={id:string;buildNumber:string;status:string;product:{name:string};unit?:{unitNumber:string}|null};
function App(){const[inventory,setInventory]=useState<InventoryRow[]>([]);const[builds,setBuilds]=useState<Build[]>([]);useEffect(()=>{fetch('http://localhost:4000/inventory').then(r=>r.json()).then(setInventory).catch(()=>{});fetch('http://localhost:4000/builds').then(r=>r.json()).then(setBuilds).catch(()=>{});},[]);const lowStock=inventory.filter(x=>x.available<=3);return <div className="app"><aside><h1>GODMODE OPS</h1><nav>Dashboard<br/><b>Inventory</b><br/>Products & BOMs<br/>Builds<br/>Units<br/>Purchasing<br/>Warranty</nav></aside><main><header><div><h2>Operations Dashboard</h2><p>Inventory, BOMs and production</p></div></header><section className="cards"><article><span>Inventory SKUs</span><strong>{inventory.length}</strong></article><article><span>Low Stock</span><strong>{lowStock.length}</strong></article><article><span>Active Builds</span><strong>{builds.filter(b=>!['COMPLETED','CANCELLED'].includes(b.status)).length}</strong></article><article><span>Completed Units</span><strong>{builds.filter(b=>b.unit).length}</strong></article></section><section className="panel"><h3>Production</h3><table><thead><tr><th>Build</th><th>System</th><th>Status</th><th>Unit</th></tr></thead><tbody>{builds.map(b=><tr key={b.id}><td>{b.buildNumber}</td><td>{b.product.name}</td><td>{b.status}</td><td>{b.unit?.unitNumber??'—'}</td></tr>)}</tbody></table></section><section className="panel"><h3>Inventory</h3><table><thead><tr><th>SKU</th><th>Component</th><th>On hand</th><th>Reserved</th><th>Available</th></tr></thead><tbody>{inventory.map(r=><tr key={r.skuId+r.locationId}><td>{r.sku?.code}</td><td>{r.sku?.name}</td><td>{r.onHand}</td><td>{r.reserved}</td><td>{r.available}</td></tr>)}</tbody></table></section></main></div>};createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
